'use client';
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function AnalTab() {
  return (
    <Card>
      <CardHeader><CardTitle>Anal</CardTitle></CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">Accès aux contenus adhérents (à venir).</p>
      </CardContent>
    </Card>
  );
}
